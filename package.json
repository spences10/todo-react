{
  "name": "todo-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prop-types": "15.6.2",
    "react": "16.6.3",
    "react-dom": "16.6.3"
  },
  "devDependencies": {
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -rf todo-react && react-scripts build && mv build todo-react",
    "deploy": "now todo-react/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
